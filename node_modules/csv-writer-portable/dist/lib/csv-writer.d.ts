import { CsvStringifier } from './csv-stringifiers/abstract';
export interface IFileWriter {
    write(string: string): Promise<void>;
}
export declare class CsvWriter<T> {
    private readonly csvStringifier;
    private append;
    private readonly fileWriter;
    constructor(csvStringifier: CsvStringifier<T>, path: string, encoding?: string, append?: boolean, useBom?: boolean, fileWriter?: IFileWriter);
    writeRecords(records: T[]): Promise<void>;
    private get headerString();
}
